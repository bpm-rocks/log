[package]
name=log
version=1.0.0
include=liblog
description=Add logging capabilities to your programs. Send messages to stdout, stderr, a log file and syslog.

[dependencies]
array=*
string=*

[devDependencies]
embed-docs=*
unittest=*

[scripts]
readme=embed-docs README.md libis
test=unittest test/*.test
